

-- Tablas de Regions

SELECT * FROM REGIONS WHERE REGION_ID=REGION_ID;
SELECT * FROM REGIONS;
INSERT INTO REGIONS (REGION_ID,REGION_NAME) VALUES (REGION_ID,"REGION_NAME");
UPDATE REGIONS SET REGION_ID=REGION_ID,REGION_NAME="REGION_NAME" WHERE REGION_ID=REGION_ID;
DELETE * FROM REGIONS WHERE REGION_ID=REGION_ID;



-- PROCEDIMIENTOS --

CREATE OR REPLACE PROCEDURE proc_insert_regions(v_region_id NUMBER,v_region_name VARCHAR2)
AS 
BEGIN 
INSERT INTO Regions (REGION_ID,REGION_NAME) VALUES (v_region_id,v_region_name);    
END;

